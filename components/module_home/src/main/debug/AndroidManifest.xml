<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.gyzq.module_hq"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="guoyuan.szkingdom.android.module_home.AppApplication"
        android:allowBackup="true"
        android:appComponentFactory="android:appComponentFactory"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gycomponentmvvm"
        tools:replace="android:appComponentFactory">
        <activity android:name="guoyuan.szkingdom.android.module_home.HomeMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>